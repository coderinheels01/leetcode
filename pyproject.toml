[project]
name = "leetcode"
version = "0.1.0"
description = "LeetCode problem solutions in Python"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
